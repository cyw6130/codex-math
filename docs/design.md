# 设计原则

三个独立持久 Codex session 平级承担主要推理；Main 负责规划、回收、统一接纳和共享事实文章，Sol/medium 原生子 Agent 独立审核。

setup-math 自动创建并绑定三个同项目 local 任务，直接询问初始模型与推理强度，创建后允许用户自行修改，不重新对齐。run-math 保留七阶段研究交互：启动讨论、核对材料、准备上下文、派发、等待与预算、按轮接纳、收尾理解。

轮次预算为硬上限，接纳必须有独立审核和真实证据；地图及 Lean 形式化按需进入。运行时验证研究状态与证据记录的一致性。
