# 使用说明：Codex 数学研究工作流

本套装用三个独立 Codex 任务（session）承担数学推理，由当前主任务 Main 负责组织研究、回收结果和维护文章。

两个主要入口：

- `setup-math`：配置模型、创建并绑定三个证明任务、设置研究预算和准备研究基线。
- `run-math`：开展研究，按轮审核结果、更新事实文章，最后与你共同理解所得。

## 一、放到目标电脑上

解压整个压缩包，将仓库文件夹放在一个固定位置。保留其内部结构，尤其是同级的 `skills/`、`runtime/`、`tools/` 和 `vendor/`；不要只复制单个 SKILL.md。

运行环境需要：

- 支持创建、读取、发送和等待其他任务，以及定时接续能力的 Codex 桌面宿主。
- Python 3.10 或更新版本，以及 Node.js。
- 已在 Codex 中保存的研究项目。

这不是普通 ChatGPT 网页对话或只有单任务能力的 CLI 工作流。缺少所需任务工具时，setup 会说明缺项，不能用三个普通聊天冒充。

### 不安装也可以先使用

在目标研究项目中打开一个 Codex 主任务，把实际路径提供给它，例如：

> 请读取 `/实际存放位置/codex-math/skills/setup-math/SKILL.md`，按这个技能配置当前项目的数学研究。使用这套独立版本，不修改其他版本的安装。

路径请换成你解压后的真实位置。直接读取方式不要求输入框已经识别 `$setup-math`。

### 希望通过技能名称调用

可以让目标电脑上的 Codex 按其宿主支持的技能安装方式，为 `skills/` 下含 SKILL.md 的各个技能目录建立链接，并保留套装目录不动。`skills/shared/` 是公共资源，不是单独的技能入口。

安装前先检查同名技能，确认它们指向本套装的真实目录。安装后的技能发现方式以目标宿主为准。

## 二、首次配置：setup-math

技能可被识别后，在主任务中输入：

```text
$setup-math
```

setup 会依次处理：

1. 询问三个证明任务使用的模型与推理强度。只能选择目标宿主实际支持的组合。
2. 确认分工、研究轮次预算和结果接纳方式。预算推荐 3 轮；推荐按轮自动数学 Commit，但须有本项目用户的真实授权。
3. 在当前已保存项目中自动创建三个独立 Codex 任务，使用同一项目目录，核验身份并保存绑定。无需手动逐个提供聊天引用。
4. 询问是否根据项目内的本地材料建立研究基线；只导入你选定的材料。已有事实工作区则核对和恢复，不覆盖历史。

三个任务创建时只确认待命，不开始数学研究。只调用 setup 不会自动运行研究。

如果暂时没有研究材料，可以选择不导入；之后 run-math 会先与你明确目标、定义和约定，再完成研究起点的准备。不能把空模板当作已经审核的事实基线。

### 已经配置过

再次调用 setup 会核验并复用已有绑定，不重复创建三个任务。创建过程中断时保留成功项并核对不确定项；不要因为没看到回复就反复要求创建。

## 三、开始研究：run-math

例如：

```text
$run-math 我希望证明：……
```

或者：

```text
$run-math 我希望探索……这个对象，重点理解……
```

未安装技能时，也可要求主任务读取实际路径下的 `skills/run-math/SKILL.md`，并给出同样的研究需求。

工作流包含七个阶段：

1. 确认研究意图与模式。
2. 核对配置与既有材料。
3. 准备轮初上下文与派发决定。
4. 派发与恢复。
5. 等待、预算与轮次屏障。
6. 按轮整理与接纳。
7. 收尾与查看。

第一次研究会通过 grilling 与你商定精确目标、关键约定和首轮路线。回答在对话正文中进行。已有真实共识可以复用；同一目标与预算内的后续轮次不必逐轮批准。原则性目标或假设变化仍需与你讨论。

## 四、三个任务如何协作

三个证明任务平级，由 Main 按当轮问题分工。它们可以读取授权材料、运行验证，并在各自指定目录保存实验与完整结果。

共同事实文章、研究状态和接纳记录由 Main 统一维护。候选结果须经过独立审核才能接纳；三个证明任务不能自行宣布其候选成为已证明前提。原有 Sol/medium 原生子 Agent 独立审核分工仍保留；setup 所选模型用于三个证明任务，不自动改变审核者配置。

多个任务在同一轮并行派发仍只计一轮。等待、Main 局部修订、独立复核和文章维护不另计推理轮。预算耗尽后继续完成已授权收尾，不自动开启新一轮研究。

Main 活跃时使用任务等待工具及时回收结果；定时接续用于长任务和中断恢复。单项研究任务从发送开始最多等待 120 分钟。超时或失败会如实保存状态，不把部分结果伪装成完整完成。

## 五、修改模型与继续研究

你可以在各证明任务中自行修改模型和推理强度。之后 setup 和 run 都不会自动重新对齐，也不会覆盖这些修改。配置中显示的创建时模型只是历史记录，未必是当前模型。

继续已有研究时，在 Main 中说明：

```text
继续上次的研究。
```

如果原预算已经用完，请同时说明追加轮数，例如：

```text
沿用已确认的目标和约定，再研究 2 轮。
```

已在途任务或未完成文章收尾会先恢复，不因“继续”重复派发。

## 六、成果在哪里

默认情况下：

| 位置 | 内容 |
|---|---|
| `.codex/math-sessions.json` | 三个任务的正式绑定与项目配置 |
| `.codex/math-sessions.setup.json` | 创建与绑定过程的恢复记录 |
| `.codex/run-math/` | 研究调度与模式状态 |
| `research/results/<目标>/round-<轮数>/` | 原始任务、结果、轮次总结和派发记录 |
| `facts/` | 事实工作区；具体位置以配置为准 |
| `research/teaching/` | 研究结果的共同理解记录 |

归档目录保存研究证据；状态与 schema 字段的约束见技能合同。

这里的数学 Commit 是审核后的成果接纳，不是 Git 提交或推送。事实文章按轮维护；数学地图在理解结果后按你的选择生成，Lean 蓝图和形式化也按需进入。

## 七、使用边界与检查

- 两个版本不要同时调度同一个事实工作区。
- 不要删除不确定是否仍在使用的证明任务或恢复记录；先让 Main 核对。
- 工具报错、要求输入或审批、只返回摘要，都不等于数学任务已完整完成。
- 首次在目标电脑使用时，按 [目标宿主核验清单](verification/target-host-checklist.md) 检查创建、复用、模型修改与结果归档。

本版已通过 16 个技能结构检查、51 项 Node 测试、141 项 Python 测试及独立审查。没有在制作电脑上安装或实发三个研究任务，因此这些检查不替代目标宿主端到端验证，也不是数学正确性证明。

更多说明：[README](README.md) · [设计决定](docs/design.md) · [验证记录](verification/report.md)。

## 八、按需查询与工程反馈

不理解工作流术语时，可以直接使用 `$dictionary`，例如：

```text
$dictionary 一个研究轮次如何计数？审核与文章收尾也算一轮吗？
$dictionary 候选结果、已接纳结果和文章已发布有什么区别？
```

无需先 setup，查询不会启动研究。已有数学证明的讲解仍使用 understand-research。

维护本仓库时可用项目级 `$issue <想法或变更>` 整理工程反馈。该技能位于 `.agents/skills/issue/`，不随 skills/ 链接自动安装到每个研究项目。它先查重、展示草稿并取得发布确认，不会因为研究产生新结论而自动发 Issue。
